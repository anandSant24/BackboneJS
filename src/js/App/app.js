define(['jquery',
		'underscore',
		'backbone'
	],function($, _ ,Backbone){

	var dayTaskMainView = Backbone.View.extend({
		
		render: function(){

		}
	});

	return {
		dayTaskView : dayTaskMainView
	}
});